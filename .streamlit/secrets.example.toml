# Copy to `.streamlit/secrets.toml` and fill.
# NEVER commit secrets.toml.

DATABASE_URL="sqlite:///local.db"
# DATABASE_URL="postgresql+psycopg://user:pass@host:5432/dbname"

# Optional: Lens API
LENS_API_TOKEN=""

# Optional: Stripe
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_ID=""

# Optional: simplest private gating (MVP)
DASHBOARD_PASSCODE=""
